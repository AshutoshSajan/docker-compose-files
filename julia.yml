services:
  julia-docker:
    image: julia:1.10
    working_dir: /work
    stdin_open: true
    tty: true
    ports:
      - '${JULIA_PORT:-7777}:7777'
    volumes:
      - './julia-work:/work'
    # Default to an interactive REPL. Override for scripts, e.g.:
    # command: ["julia", "hello.jl"]
